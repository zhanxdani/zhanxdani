#include <stdio.h>

extern long max_div(long* arr, int count);

int main() {
    long arr[] = {36, 72, 144};
    int count = sizeof(arr) / sizeof(arr[0]);
    long result = max_div(arr, count);
    printf("Maximum common divisor is: %ld\n", result);
    return 0;
}
